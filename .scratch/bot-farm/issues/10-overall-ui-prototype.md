Type: prototype
Status: claimed

## Question

Build a rough multi-artboard UI prototype (via `/design`) covering one representative screen per pillar — playbooks, project tracker, agent fleet/orchestration, laws & permissions, bots (including bot config + memory audit list) — plus a shared nav shell, to raise the fidelity of discussion before locking any screen-level decisions. Not high-fidelity; a concrete artifact to react to and split off dedicated Prototype tickets from wherever it surfaces something needing deeper iteration.
