{
  "name": "lings",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @lings/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20"
  }
}
